# fah-hub-client — moved

The flutter_agent_harness adapter lived here as `adapters/fah-hub-client/`. It is
now its own repository: https://github.com/vabhzw17eg2qu4m9-bit/fah_hub_client
(pub.dev: `fah_hub_client`). Its quality gates (dart analyze/test, crap4dart
≤ 12, coverage/CRAP ratchets, publish pipeline) live in that repo.
