# omp-extension — moved

The oh-my-pi (omp) adapter lived here as `adapters/omp-extension/`. It is
now its own repository: https://github.com/vabhzw17eg2qu4m9-bit/omp_hub_client
(npm: `omp_hub_client`). Its quality gates (tsc --noEmit, npm test, coverage +
duplication ratchets, publish pipeline) live in that repo.
